import React from "react"

/** A function that should able to call the API from given payload, 
 *      if there are failures, display the error
 *      if success, display the success message
 *      return error messages and success response
 */
interface IButtonAction {
    
}
export const buttonAction = () => {
}