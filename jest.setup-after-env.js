// jest.setup-after-env.js
// This runs after the test framework is initialized, so `expect` is available.

// Add jest-dom matchers (toBeInTheDocument, toHaveTextContent, etc.)
require('@testing-library/jest-dom');
